# Seeders

Los datos iniciales se cargan desde `backend/app/services/seed_data.py` al iniciar la API.

Incluye roles, permisos, usuario administrador, zona, cliente, dirección, repartidor de demostración y cinco puntos de interés para validar consultas geoespaciales.
